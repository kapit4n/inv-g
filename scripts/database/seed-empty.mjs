#!/usr/bin/env node
console.log(`🌱 Empty profile needs no business seed.
   The app creates users/roles/permissions/settings automatically on launch against:
   inventory-gear-empty.db`)
process.exit(0)